import 'package:flutter/material.dart';

class ExercisButtonWidget extends StatelessWidget {
  const ExercisButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}